name: first-workflow
on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome to first workflow 
        run: |
            echo "testing"
             sleep 30 
      - name: echoing
        run: echo "building"

      - name: deploy
        run: echo "Deployment done"
