name: first-workflow
on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Building
              run: |
                echo "bulding"
                sleep 30

    second-job:
        runs-on : ubuntu-latest
        steps:
            - name: deploying
              run: |
                echo "deploying"
                sleep 30